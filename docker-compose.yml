services:
  lattice:
    build: .
    container_name: lattice-app
    ports:
      - "5173:5173"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
